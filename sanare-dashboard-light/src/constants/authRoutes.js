export const ROLE_HOME = {
  clinician: '/clinician/dashboard',
  patient: '/patient/home',
  surgeon: '/surgeon/dashboard',
}

/** Shareable entry URLs — one link per portal for user feedback sessions. */
export const PORTAL_ENTRY = {
  clinician: '/pt',
  patient: '/patient',
  surgeon: '/surgeon',
}

export const PORTAL_LABELS = {
  clinician: 'Physiotherapist Portal',
  patient: 'Patient Portal',
  surgeon: 'Surgeon Portal',
}
